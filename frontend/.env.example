# Copy to .env.local for development. Never commit real values.
# Backend REST base URL and WebSocket URL.
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
# Base path of the deployed site: "/svp/" on GitHub Pages, "/" when served by the backend.
VITE_BASE_PATH=/
